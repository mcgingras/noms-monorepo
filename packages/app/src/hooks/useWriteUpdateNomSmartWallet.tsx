// Returns contract write function to batch mint then equip new traits
// For use with a smart wallet, which bundles the transactions into a single call
const useWriteUpdateNomSmartWallet = () => {
  //
};

export default useWriteUpdateNomSmartWallet;
